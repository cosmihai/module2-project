<div class="column is-4">
  <div class="card">
    <header class="card-header">
      <p class="card-header-title is-centered">
        {{event.name}} 
      </p>
    </header>
    <div class="card-content">
      <div class="content">
        {{!-- {{event.description}} --}}
        <p>
          <time datetime="2016-1-1">{{event.date}}</time>
        </p>
      </div>
    </div>
    <footer class="card-footer">
      {{#unless user}}
        <a href="/auth/login" class="card-footer-item">See more</a>   
      {{/unless}}   
      {{#if user}}
        <a href="/user/{{event._id}}" class="card-footer-item">See more</a>
      {{/if}}
    </footer>
  </div>
</div>