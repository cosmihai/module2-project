<section class="detail-info">
  <div class="container">
    <div class="columns">
      <div class="column is-1"></div>
      <div class="column is-two-thirds content container">
        <p>
          <span class="title is-bold has-text-light">
            {{event.name}}
          </span>
          <!---->
        </p>
        <p class="subtitle has-text-light">
          Description:
        </p>
        <p class="has-text-light">
          {{event.description}}
        </p>
        <p class="subtitle has-text-light">
          Date:
          <span class="has-text-light is-size-6">
            <time datetime="2016-1-1">{{event.date}}</time>
          </span>
        </p>
        <p>
          Created by
          <a class="has-text-success" href="/users/{{event.owner._id}}">{{event.owner.username}}</a>
        </p>
        <p class="subtitle has-text-light">Who is coming:</p>
        <p>  
          {{#each event.attendants}}
          <a class="has-text-light" href="/users/{{this._id}}">{{this.username}}</a>, 
          {{/each}}
        </p>
      </div>

      <div class="column is-4">
        <div class="field is-grouped detail-btn container">
          {{#if buttonPermission}}
          <p class="control">
            <a href="#" class="button is-light">
              Edit
            </a>
          </p>
          <p class="control">
            <form method="POST" action="{{event._id}}/delete">
              <button class="button is-light">Delete</button>
            </form>
          </p>
          {{else}} {{#if joinedEvent}}
          <p class="control">
            <form method="POST" action="{{event._id}}/unjoin">
              <button class="button is-danger">Unjoin</button>
            </form>
          </p>
          {{else}}
          <p class="control">
            <form method="POST" action="{{event._id}}/join">
              <button class="button is-danger">Join</button>
            </form>
          </p>
          {{/if}} {{/if}}
        </div>
      </div>
    </div>
  </div>
</section>

<section>
  <div class="box has-text-centered container">
    Here goes the map
    <div id="map"></div>
  </div>
</section>